#include <stdio.h>

int main() {
    int x,y;
    scanf("%d %d",&x,&y);
    int w = (2*x - y)*506;
    if((w<0)||x>y){
        printf("%d",-1);
    }
    else
        printf("%d",w);
}

이 문제는 수학적으로 접근해서 풀어야 하는 문제이다.
X와 Y를 중앙에서부터 거리를 놓고 계산하면 4W는 2X-Y가 된다
고로 W에 2024를 곱한건 2X-Y에 506을 곱한 것이고
만약 W값이 0보다 작을경우엔 -1을 출력, 그렇지 않으면 W값을 출력한다
